# OD-01 供应商选型（预筛评估与决策前置记录）

> 文档状态：**预筛完成 / 待沙箱实测 / 待三方签字**。本文所有桌面评分均为预筛估计，**不构成最终决策**；最终结论以沙箱实测数据修订后的评分卡为准。

> **2026-08-03 区域范围调整（项目负责人确认）**：eu/intl 区调整为「待开发」，本轮 Phase 0 实测与供应商选型以 **cn 区**为唯一在测范围；eu/intl 候选名单保留作为后续窗口输入，不进入本轮实测与评分。

> **2026-08-03 自建矩阵定稿（项目负责人确认，待实测验收）**：
> - WebRTC/SFU：自建 LiveKit（本地开发先行，云上统一部署后续）；STUN/TURN：自建 coturn；录制/转推：自建 LiveKit Egress + FFmpeg；媒体存储：自建 MinIO（S3 兼容）。
> - ASR：自研自建 FunASR/SenseVoiceSmall（CPU 回合级转写，2026-08-03 确认；实时 API 留作后续升级）。
> - TTS：自研自建（CosyVoice 2 或 F5-TTS 自托管；开发期用轻量模型冒烟）。
> - LLM：接 API（DeepSeek）。
> - 数字人：自建（静态形象 + 音频播放 MVP，不追求实时口型；口型驱动列为后续升级项）。
> 商业供应商（腾讯 TRTC/讯飞/腾讯数智人等）降为实测对照备选，本轮不注册付费账号；最终以沙箱实测评分卡与三方签字为准。

## 1. 元信息

- 文档编号：OD-01-EVAL-001
- 版本：0.2.0（2026-08-03，预筛结论版；实测后升版定稿）
- 追踪：IMPLEMENTATION_PLAN.md 第 7 节 OD-01；TEST-PHASE0-001；NFR-007 ~ NFR-012；TASK-030、TASK-091、TASK-096
- 一致性锚点：docs/ai/PROVIDER-ADAPTERS.md（能力契约）、docs/architecture/DEPLOYMENT.md（区域与故障切换）、docs/testing/PHASE0-PROVIDER-EVALUATION.md（评测方法）、fixtures/synthetic/（评测数据）
- 决策负责人：技术负责人、AI 负责人、安全负责人（三方签字）

## 2. 目的与范围

将未决事项 OD-01（WebRTC/SFU、ASR、TTS、LLM、实时数字人主备供应商）推进到可实测、可决策状态：

- 五类能力分别给出候选名单（每类 ≥2 家，按区域 cn/eu/intl 给出主/备建议，待实测确认）。
- 全部候选必须先过 Phase 0 准入门槛，再进入评分；红线项一票否决。
- 本文为窗口 2（TASK-091/092/096 真实环境验证）的唯一前置：沙箱实测完成后回填评分卡、三方签字、形成 ADR，OD-01 方可关闭。

## 3. 评估方法与门槛（引用 Phase 0 方案）

- 评分口径：每类能力 100 分制，六维权重：实时性 30 / 质量 25 / 数据区与合规 15 / 授权与品牌安全 10 / 故障与可替换性 10 / 单位成本 10。
- 准入门槛（任一不满足即淘汰）：
  - 支持在用户所属数据区内处理（或提供区域化部署/端点）；中国区内容不默认出境；
  - 可签 DPA，承诺不留存用户内容用于模型训练；
  - 数字人候选必须提供固定授权角色库与完整肖像/声音授权链；不支持每场生成新脸、不做未授权克隆；
  - 提供沙箱/试用环境与可核算的单价模型；
  - 安全自评：传输加密、密钥管理、SOC 2/ISO 27001 或等效证明。
- 红线（一票否决）：数据默认出境、拒绝 DPA 零保留、授权链不完整、无法满足打断 P95 ≤ 500ms（数字人/ASR 链路合计）、无法提供固定角色库。
- 切换约束：故障演练验证熔断与切换，切换不得跨数据区。

## 4. 桌面预筛结论（待实测修订）

以下为 2026-08-03 桌面调研结果。评分仅用于缩小实测名单，实测后按 Phase 0 六维权重重新打分并回填。

### 4.1 WebRTC/SFU

- 候选：声网 Agora、腾讯云 TRTC、LiveKit Cloud（可自托管）、100ms。
- cn 区主/备建议：自建 LiveKit（主，本地开发先行，云上统一部署后续）/ 腾讯云 TRTC（备）。
  - LiveKit：开源 SFU（Apache-2.0）且为项目架构技术基线（PRD、SYSTEM-ARCHITECTURE、services/room），可自托管；媒体流数据自持，契合 cn 区数据合规叙事；本地开发零账号成本，云上统一部署后按 NFR-004 三可用区扩展。
  - TRTC：腾讯生态内集成便利、全国节点成熟，作为商业兜底备选；按分钟计费。
  - Agora：保留为候选（本轮不注册、不实测）。
- eu/intl 区主/备建议：LiveKit Cloud（主）/ 100ms（备）。（待开发，本轮不实测）
  - LiveKit：提供 EU 区域部署、可自托管，自托管可满足数据驻留审计需求；区域固定，实测需确认目标区域可用性。
  - 100ms：区域覆盖好、按参与者分钟计费，单价量级约 $0.004/参与者分钟。
- 预筛关注点：建连成功率和时延指标（95% ≤ 8s、99% ≤ 15s）、弱网降级行为、录制与旁路能力、单价模型透明度。

### 4.2 ASR

- 候选：阿里云实时语音识别、讯飞、Deepgram（EU endpoint 已 GA、可自托管）、Azure Speech、OpenAI Realtime/Whisper。
- cn 区主/备建议：自研自建 ASR 服务（主）/ 讯飞（备，对照）。
  - 自建：FunASR/SenseVoiceSmall 自托管（CPU 回合级）；本机实测 12 秒音频转写约 0.6 秒，中文质量以实测为准；NFR-010 实时 1s 门槛作为后续实时 API 升级项。
  - 讯飞：作为对照备选，仅实测对比时启用。
- eu/intl 区主/备建议：Deepgram（主）/ Azure Speech（备）。（待开发，本轮不实测）
  - Deepgram：EU 端点 GA、支持自托管、零保留配置；英文质量与延迟指标优秀。
  - Azure Speech：区域矩阵完整、与 Azure OpenAI 同栈，合规材料齐全。
- 预筛关注点：中英文及口音集字准率门槛（由 AI 负责人随校准基线确定）、最终文本时延 P95 ≤ 1s、方言覆盖（cn 区）。

### 4.3 TTS

- 候选：火山引擎豆包大模型语音合成、讯飞、ElevenLabs（EU data residency + Zero Retention）、Azure TTS。
- cn 区主/备建议：自研自建 TTS 服务（主）/ 讯飞（备，对照）。
  - 自建：CosyVoice 2 或 F5-TTS 自托管；开发期用轻量模型冒烟，自然度以盲评验收。
  - 讯飞：作为对照备选，仅实测对比时启用。
- eu/intl 区主/备建议：ElevenLabs（主）/ Azure TTS（备）。（待开发，本轮不实测）
  - ElevenLabs：EU 数据驻留与零保留配置明确，多语种自然度高。
  - Azure TTS：企业合规成熟、区域矩阵完整。
- 预筛关注点：MOS 自然度盲评、音色授权范围（商用用途）、口型同步时延（与数字人链路合计 ≤ 200ms）、长文本稳定性。

### 4.4 LLM

- 候选：DeepSeek、通义千问 Qwen、讯飞星火、Azure OpenAI、Claude、Gemini。
- cn 区主/备建议：DeepSeek API（主）/ 阿里云百炼 Qwen API（备）。
  - DeepSeek：单价显著低、中文质量强；**仅限 cn 区使用，eu/intl 属红线**。
  - Qwen：阿里生态、合规与部署方式灵活。
- eu/intl 区主/备建议：Azure OpenAI（主）/ Claude（备）。（待开发，本轮不实测）
  - Azure OpenAI：支持 Sweden/Switzerland 等区域部署、区域模型矩阵明确、DPA 与安全材料齐全。
  - Claude：多语种推理质量强，作为可替换备选。
- 预筛关注点：追问相关性、内容安全、评分链路确定性（温度与采样参数）、数据驻留配置、单位 Token 成本模型。

### 4.5 实时数字人

- 候选：腾讯云智能数智人、硅基智能、HeyGen、Synthesia。
- cn 区主/备建议：自建数字人（主，静态形象 + 音频播放 MVP；不追求实时口型）/ 腾讯云智能数智人（备，对照）。
  - 自建：固定授权形象图 + 音频播放；口型驱动（NFR-011）列为后续升级项，不阻塞本期。
  - 腾讯云：作为对照备选，仅实测对比时启用。
- eu/intl 区主/备建议：HeyGen（主）/ Synthesia（备）。（待开发，本轮不实测）
  - HeyGen：商用角色库与授权条款清晰、API 完善。
  - Synthesia：企业级合规与固定角色库支持。
- 红线核验：固定授权角色库 + 完整肖像/声音授权链；禁止"每场生成新脸"与未授权克隆（含竞品演示中常见的任意克隆能力，不作为候选）。
- 预筛关注点：口型/音频偏差 ≤ 200ms、打断 P95 ≤ 500ms、长会话（60 分钟）稳定性、授权按角色还是按席位计费。

## 5. 成本估算（量级，待实测核价）

- 本轮仅核算 cn 区；eu/intl 估算保留为后续窗口参考，不计入本轮决策。
- 自建矩阵生效后成本结构变化：ASR/TTS/SFU/数字人为服务器固定成本（GPU 机月租或自购），LLM 按 API 量计费；原分钟级估算仅作商业对照参考。
- cn 区全链路（SFU + ASR + TTS + LLM + 数字人月费摊薄）：量级约 **¥0.1 ~ 0.3/分钟/面试**（1 对 1 场景，30 分钟标准面试）。
  - ASR 约 ¥0.02/分钟量级、TTS 约 ¥0.02 ~ 0.05/分钟、SFU 约 ¥0.02 ~ 0.06/参与者分钟、LLM 按输入/输出 Token 折算约 ¥0.01 ~ 0.05/分钟、数字人按角色/月费摊薄。
- eu/intl 区全链路：量级约 **$0.1 ~ 0.25/分钟/面试**。
  - SFU 约 $0.004 ~ 0.024/参与者分钟、ASR 约 $0.017/分钟（OpenAI Whisper/Realtime 量级，Deepgram 按量定价）、TTS 约 $0.02 ~ 0.05/分钟、LLM 约 $0.005 ~ 0.03/分钟、数字人月费摊薄。
- 说明：以上为桌面量级估算，未含重试/失败调用与带宽成本；实测阶段以各供应商可核算单价模型回填，按 Phase 0 单位成本维度重新打分。

## 6. 待沙箱实测清单（窗口 2 前置输入）

- 本期实测范围：**cn 区**；eu/intl 清单保留用于后续窗口。
- 数字人音视频建连：95% ≤ 8s、99% ≤ 15s（合成对话 ×200 次/供应商/区域，冷启动计时）。
- 用户发言结束 → 数字人开始回复：P50 ≤ 1.5s、P95 ≤ 3s、P99 ≤ 5s（全链路：ASR + LLM + TTS + Avatar）。
- 打断 → 停止发声：P95 ≤ 500ms（语音打断 + 停止按钮 ×100 次）。
- ASR 最终文本时延：P95 ≤ 1s；中英文及口音集字准率达标（≥30 样本/口音集）。
- 口型与音频偏差 ≤ 200ms（录制样本对齐分析）。
- 默认视频规格 ≥ 720p/24fps；弱网（3 Mbps / 512 kbps / 256 kbps + 30% 丢包）下降级行为观察。
- 60 分钟长会话稳定性：无内存泄漏、无连接退化（合成脚本长跑 ×10）。
- 故障演练：断网/限流/超时/熔断切换（closed/open/half_open）、活跃会话状态恢复；切换不跨区。
- 合规与授权核验：数据区处理证据、DPA/零保留配置、数字人固定角色库与授权链证明。

## 7. 决策路径

1. 五类能力沙箱接入（仅通过供应商适配层，禁止业务代码直连）。
2. 合成数据基准测试（质量）+ 指标矩阵实测（第 6 节）+ 故障演练。
3. 评分卡打分 + 成本核算，原始测量数据入库（ai/evals/ 新增供应商评测目录，数据标记 synthetic）。
4. 安全/授权复核（安全负责人）。
5. 技术、AI、安全负责人三方签字 → OD-01 决策记录定稿 → 对应 ADR 入库。
6. 更新 IMPLEMENTATION_PLAN.md OD-01 状态为已决策，解锁窗口 2（TASK-091/092/096 真实环境验证）与 TASK-030 供应商接入。

## 8. 风险与缓解

- 数字人授权链不完整（P0 风险，肖像/深度伪造）：任何候选未提供完整授权链即否决；OD-03（法律实施方案）联动核验。
- DeepSeek 仅限 cn 区：eu/intl 路由禁止进入 DeepSeek，配置层强制区域隔离。
- 自建数字人 MVP 无实时口型：接口与 UI 按降级模式明示；后续升级口型驱动后回填 NFR-011 实测。
- 自建 LiveKit 运维责任自担：本地开发单机即可起步；云上部署需完成 TURN、证书、监控与三可用区容灾，并验证 NFR-004（单 AZ 60s 接管、区域 RTO ≤ 30min）。
- LiveKit Cloud 区域固定：实测前确认目标区域可用性；若不可满足则以自托管或 100ms 为主。
- 供应商单价模型不透明：无法核算成本则单位成本维度计 0 分，并在决策记录标注。
- 实测数据与供应商承诺严重不符：以实测为准并记录；两候选总分接近（差 < 5 分）时以红线项与故障演练结果为决胜项。
- 评测期间供应商服务降级：记录为故障样本，不因此延长评测窗口。

## 9. 签字栏（实测完成后签署）

- 技术负责人（签名 / 日期）：__________________
- AI 负责人（签名 / 日期）：__________________
- 安全负责人（签名 / 日期）：__________________

签字完成且 ADR 入库后，本文升级为定稿（0.3.0），OD-01 关闭。

> 评分卡建议分与完整签字材料见 `docs/decisions/OD-01-signoff.md`（2026-08-04 待三方签字）。

## 10. 自建落地开发计划（2026-08-03）

### M0 决策与文档（已完成）

- 自建矩阵定稿并入库；provider-eval 工具箱与 LiveKit 本地开发模块合入。

### M1 本地自建骨架（当前阶段）

- SFU：LiveKit 本地运行（已跑通，ws://localhost:7880）。
- ASR/TTS：mgd-selfhost 已接入 FunASR/SenseVoiceSmall（CPU 后端）并跑通「合成 → 转写」闭环；TTS 开发期用 piper，生产候选 CosyVoice 2 / 火山 API。
- 媒体存储：MinIO 本地实例（S3 兼容，媒体桶）。
- Avatar：静态形象 + 音频播放 MVP（授权形象图 + avatar driver 静态实现）。
- 集成：services/room 接入 LiveKit Provider；services/asr/avatar 接入自建端点；前端接 WEBRTC_* 环境变量。
- 验收：本机全链路（浏览器 → LiveKit → ASR/TTS/数字人）可走通一个面试回合。

### M2 实测验收（OD-01 第 6 节）

- 第一轮本地实测（2026-08-03）：ASR 回合级 P50 404ms / P95 856ms，10 条合成样本平均相似度 0.965；TTS（edge-tts，标准单声道 24k WAV）整段合成 P95 约 3.4s；LLM（DeepSeek）3/3 通过。样本与报告见 `ai/evals/providers/samples/` 与 `ai/evals/providers/reports/m2-local-round1.json`。
- TTS MOS 人工盲评完成（项目负责人，2026-08-04）：10/10 满分，平均 MOS 5.0，已回填报告 `blind_review` 字段。
- 第二轮浏览器 E2E（2026-08-03，自建 LiveKit 双端合成音视频）：建连 P95 1.4s / P99 1.5s（NFR-007 达标）；远端视频 1280×720 @23.98fps（720p/24fps 达标），帧间隔 P95 42ms；首帧 365ms；本地播放停止响应 0.1ms（VAD 打断链路待接媒体管线）。样本与报告见 `samples/webrtc_sfu_cn_livekit.jsonl` 与 `reports/m2-e2e-round2.json`。待测：弱网降级、60 分钟长会话。
- VAD 打断模块已接入前端（2026-08-04）：麦克风 → 本地 RMS/VAD（attack/hangover 去抖）→ 数字人停止发声，房间页提供「打断演示」开关；打断时延 E2E 测量列入第三轮。
- 第三轮实测（2026-08-04）：60 分钟长会话达标——86,428 帧零断流、平均 23.998fps、帧间隔 P95 42ms、无断连无报错（`reports/m2-e2e-soak60.json`）；VAD 打断时延 P95 240ms ≤ 500ms（NFR-009 达标，注入合成语音 10 次试验，`reports/m2-interrupt-round3.json`）。待测：弱网降级（留云上公网）。
- 待浏览器联调轮次：建连、全链路回应（含数字人）、打断、720p/24fps、弱网、60 分钟长会话；TTS MOS 人工盲评（盲评包 `work/m2/audio/` + `review-manifest.json`）。
- 按 §6 清单实测：建连、全链路回应、打断、ASR 终稿、720p/24fps、弱网、60 分钟长会话；
- TTS/ASR 质量盲评（MOS/字准率）；评分卡回填；技术/AI/安全三方签字；ADR 入库；OD-01 关闭。

### M3 云上统一部署

- GPU 服务器（ASR/TTS/数字人推理）+ 通用服务器（LiveKit/coturn/MinIO/PostgreSQL/Temporal）；
- 域名 + TLS + TURN；三可用区与 60s 接管基线（NFR-004）；监控与告警接入。

### M4 窗口 2

- TASK-091 WebRTC 混沌与故障演练；TASK-092 2 倍峰值压测与区域容灾；TASK-096 三数据区生产验证与放量守门（cn 先行）。

### 测试门禁

- 每个里程碑以 §6 指标与 RELEASE-CHECKLIST 验收；自建组件与商业对照共用同一套评测脚本（ai/evals/providers）。
