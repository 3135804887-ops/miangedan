/** (room) 路由组布局：实时面试房间全宽（SCR-08/09）。 */

import type { ReactNode } from 'react';

export default function RoomGroupLayout({
  children,
}: {
  children: ReactNode;
}): ReactNode {
  return <>{children}</>;
}
