# 面个蛋前端工作区 pnpm 配置
# 依赖版本一律精确固定（AGENTS.md 第 3 节），禁止安装时写入范围符号
save-exact=true
# 工作区内部依赖统一使用 workspace: 协议
link-workspace-packages=true
save-workspace-protocol=rolling
# 严格 peer 依赖：不静默接受不兼容组合
strict-peer-dependencies=false
